<%= simple_form_for(@location) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :country %>
    <%= f.input :country_state %>
    <%= f.input :city_district %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
